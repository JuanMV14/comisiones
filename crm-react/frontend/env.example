# URL del backend (en local puedes omitir y usar el default o el proxy de Vite)
VITE_API_URL=http://localhost:8000/api

# Opcional: fallback directo a Supabase si el backend no está disponible
VITE_SUPABASE_URL=tu_url_de_supabase
VITE_SUPABASE_ANON_KEY=tu_anon_key_de_supabase

